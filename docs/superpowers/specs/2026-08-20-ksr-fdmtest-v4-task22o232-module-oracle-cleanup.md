# Spec: KSR FDM Test V4 task232 module and oracle cleanup

## Observable contract

Refactoring G-code machine preamble emission does not change generated bytes. Region composition tests retain assertions for semantic polygon membership while no longer pinning Clipper contour start positions, output-vector ordering, or encoded intermediate-stage SHA-256 streams. Every Rust source file remains below 400 lines and module splitting uses ordinary `mod` files, never `include!` or `include_bytes!` for source composition.

## Boundary

This cleanup extracts machine-limit and machine-start template emission from `project_slice/gcode_emit.rs` into `gcode_emit/machine.rs`. It removes the compile-only region interface pin block and two obsolete encoded task22J/task22K stream tests. It does not alter OrcaSlicer-derived slicing behavior or the public slicing seam.

## Acceptance

- The focused G-code emitter and region composition behavior tests pass.
- Machine preamble output remains covered through `slice_project`.
- Region subtraction and modifier partition tests compare polygon point membership independent of contour rotation and vector order.
- No Rust source file under `crates/` has 400 or more lines.
- Rustfmt and focused Clippy pass.
