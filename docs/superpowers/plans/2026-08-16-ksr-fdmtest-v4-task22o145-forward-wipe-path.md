# Plan: Task 22O.145 forward stored-path wipe traversal

1. Tighten the existing KSR inter-path travel test with the first forward wipe coordinates and retraction values; verify the reverse traversal fails it.
2. Traverse the option-gated stored loop path from its second point in source order while retaining the existing configured-distance clipping and retraction distribution.
3. Run the focused wipe test, neighboring motion contracts, strict Clippy, and rustfmt; record the next structural divergence, then commit and push.
