# Plan: Task 22O.247 arc-center Point constructor parity

1. Resolve the exact overload selected by OrcaSlicer’s two-argument `Point(center_x, center_y)` construction.
2. Restore nearest-integral scaled center conversion in Ares and a focused assertion that fails under truncation.
3. Run the focused arc-fitting test, clippy, and rustfmt checks.
4. Commit and push the corrective source-cited slice independently, then resume the KSR geometry divergence.
