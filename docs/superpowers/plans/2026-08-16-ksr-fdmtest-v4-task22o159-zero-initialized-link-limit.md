# Plan: Task 22O.159 zero-initialized monotonic link limit

1. Extend the KSR motion contract through the next exact bottom-surface contour continuation and observe the premature outer-offset/retraction failure.
2. Trace the failing generated intersection's link quality through monotonic path emission.
3. Port the normal-fill `Fill` initialization order by passing the source-zero perimeter-link limit into monotonic generation.
4. Run focused rectilinear and KSR motion contracts, rustfmt, Clippy, and file-size checks.
5. Record the roadmap milestone, commit, and push independently.
