# Task 22O Package A0 Attempt-11 Diagnostic Tooling Successor 3 Plan

## Goal

Recover from the terminal Successor-2 launch-environment defect with a new
reviewed namespace, prove cache-free ordinary PowerShell execution, publish one
approved source package, run one matrix, and resume the still-unconsumed
Attempt-11 diagnostic.

## Non-negotiable boundaries

- Never rerun or mutate Attempt 10, old Attempt 11, or Successor 2.
- Never alter Successor-2 focused approval, TDD history, runtime install,
  preflight journals, or terminal cache residue.
- Do not create `C:\a22or18-driver`, `C:\a22or18-evi`, or
  `C:\a22or18-runtime` before document approval.
- Do not execute OrcaSlicer, A0 formal/claim/candidate/campaign/collector, or
  slicing code during this recovery.
- Use bundled Python with `-B` and `PYTHONDONTWRITEBYTECODE=1`.
- Use the portable runtime only after focused approval and the sole install.
- Preserve create-once, no-follow, exact-case, closed-set, and no-retry rules.

## S3.1: Freeze Successor-2

1. Rehash Successor-2 spec, plan, focused review/envelope, installer subjects,
   TDD history, reservation, receipt, runtime, and source-review evidence.
2. Freeze the 80-file preflight evidence aggregate and the three reproduced
   driver-relative cache files.
3. Confirm no Successor-2 production failure packet, runtime ledger, source
   ledger, publication transaction, matrix, diagnostic, or A0 artifact exists.
4. Record that script-time `SystemDrive` mutation did not prevent startup cache
   creation and is not an authorized fix.
5. Never execute or clean Successor-2 again.

Verification: immutable hashes match this specification and all Successor-3
roots are absent.

## S3.2: Approve documents

1. Persist this specification and plan.
2. Send their exact bytes and hashes to the existing independent review task.
3. Require review of namespace isolation, nine-pair environment, runtime copy,
   production paths, all-C# compile coverage, one-shot transactions, cleanup,
   and continuation.
4. Repair only document findings and request a new review turn while every
   Successor-3 root remains absent.
5. After external `APPROVE`, create only `C:\a22or18-evi` and its
   `document-reviews` child; keep driver/runtime absent.
6. Render the report/envelope and exact identities in memory; create, flush,
   and same-handle read back the document-publication reservation first.
7. Persist report/envelope create-once, recursively validate them, and close
   with an independently verified terminal receipt.
8. Treat any reservation, partial output, failed/missing receipt, or schema/DAG
   fault as namespace-consuming.

Verification: approved document identities and a passed publication receipt
are immutable; only the evidence/document-review paths exist.

## S3.3: Create the mechanical driver

1. Require the passed document-publication receipt, then create ordinary
   `C:\a22or18-driver`; reuse only the approved ordinary evidence root.
2. Copy only the 92 declared Successor-2 source files, excluding the literal
   cache tree and every predecessor evidence artifact.
3. Rebind root constants from `a22or17` to `a22or18` without changing protocol
   module or kind names.
4. Freeze a closed source manifest before functional edits.
5. Create a new Successor-3 pre-install TDD history that directly embeds every
   canonical Successor-2 document/focused/install/TDD/source/preflight/cache
   identity and aggregate plus the exact runtime-closure counts/bytes/digests,
   but never modifies predecessor evidence.
6. Confirm `C:\a22or18-runtime` remains absent.

Verification: exact source set, ASCII/LF hygiene, no reparses, no cache, and
maximum 399 LOC.

## S3.4: Write RED tests

1. Add exact nine-pair manifest/schema/request/environment mutation tests.
2. Add a launch-model test proving omission of `SystemDrive` permits literal
   driver-relative cache while the nine-pair contract rejects it.
3. Add production manifest tests rejecting predecessor driver, schema,
   binding/review, ledger/results, and diagnostic paths.
4. Add bootstrap/production-DAG tests requiring one shared manifest path map.
5. Add exact final-schema identity tests: reject the 6591-byte predecessor
   schema as a Successor-3 final schema and bind newly computed reviewed bytes.
6. Add source-builder tests requiring the exact seven `.cs` paths and exact
   single-command `Add-Type -Path $args` argv, with omission, extra, duplicate,
   reordering, substitution, split-child, multiple-Add-Type, Import-Module, and
   compile-skip negatives.
7. Add native allowlist and complete production PATH contract tests.
8. Add direct predecessor-evidence tests rejecting missing/substituted,
   case-drifted, reordered, duplicate, reparse, transitive-only, and
   aggregate-drift rows, plus missing/substituted runtime-closure fields.
9. Add reservation-first fault tests for document/focused/source/publication/
   matrix/binding/tooling-review/diagnostic transactions and exact path maps.
10. Add publication role-swap tests separating approved candidate identities
    from future production paths embedded by the source ledger.
11. Run the exact synthetic gate from the Ares repository root and record RED.

Verification: failures occur at the intended boundaries before any live
runtime, publication, matrix, diagnostic, or A0 command.

## S3.5: Implement GREEN

1. Add `SystemDrive=C:` to the canonical ordered environment everywhere.
2. Derive every production tool/output path from the Successor-3 manifests.
3. Bind the current diagnostic receipt schema in the Successor-3 driver.
4. Make bootstrap use the production manifest diagnostic reservation.
5. Implement the exact one-child argv that will compile all manifest-declared
   C# sources after runtime installation.
6. Align native allowlist and production PATH assertions.
7. Run only static/pure/synthetic tests until the exact pre-install gate is
   GREEN; do not execute PowerShell, inspect `WindowsApps`, or create runtime.
8. Parse every Python file with `ast.parse`; validate every JSON artifact;
   enforce ASCII, LF-only text, no reparses, closed source set, and `<400` LOC.
9. Freeze installer-owned subjects, pre-install TDD history, source closure,
   documents, and document-publication receipt.

Verification: synthetic suite arithmetic and directly embedded predecessor
rows are exact, TEMP/cache/process state is zero, and the runtime/production
paths remain absent.

## S3.6: Focused review and runtime install

1. Render in memory the exact installer-owned set, TDD history, nine-pair
   environment, expected 1007-file runtime closure, and synthetic results.
2. For each focused-review ordinal, create/read the iteration reservation
   first, persist the rendered subjects, then send those exact bytes to the
   same independent six-axis task.
3. Obtain/persist the external verdict and close with a terminal receipt.
4. Loop on repairable `REVISE` receipts by preserving each iteration and using
   the next ordinal. Treat I/O/schema/DAG/missing-verdict/missing-receipt faults
   as namespace-consuming.
5. After one passed `APPROVE` iteration, create/read a separate finalization
   reservation first, persist the final envelope, validate all residue, and
   close with a finalization terminal receipt.
6. Confirm runtime destination, install reservation, receipt, stdout, stderr,
   diagnostic-tooling, source-review, publication, matrix, and diagnostic
   outputs are absent.
7. Execute the installer command exactly once from `C:\a22or18-driver`,
   binding the approved iteration receipt, finalization receipt, and envelope.
8. Monitor only the original process/session.
9. Independently verify reservation, receipt, 1007 copy rows, source/destination
   closure, empty raw streams, and cleanup.

Verification: one passed install receipt exists and installer-owned bytes are
frozen.

## S3.7: Real focused verification

1. Rehash runtime and source before execution.
2. Run all Successor-3 Python tests.
3. Run native and completion through journaled portable-runtime children four
   times each with the exact nine-pair environment.
4. Parse all PowerShell source four times in fresh children.
5. Compile all seven C# sources four times in fresh children.
6. Rehash runtime/source after every stage.
7. Assert no `%SystemDrive%` literal below driver/evidence, no source extras,
   empty TEMP/cache, and zero live descendants.
8. Preserve every live, failed, and superseded result as ordered create-once
   events under `post-install-tests`; never rewrite pre-install TDD.
9. Render the closed post-install evidence envelope in memory, create/read its
   reservation before parent reads, validate every event/predecessor, publish
   the envelope, and close with a terminal receipt.
10. Render and validate prospective failure packet, runtime ledger, and source
   ledger without publishing production paths.
11. Render all eight source-review candidates and their exact fixed
    `iteration-0001` output identities in memory.
12. Create/flush/same-handle-read the iteration reservation before persisting
    any candidate or verdict, then persist/read back candidates create-once.

Verification: only install evidence, focused test evidence, and one open
source-review iteration exist.

## S3.8: Source approval loop

1. Freeze source count, bytes, ordered digest, max LOC, schemas, TDD, journals,
   prospective production bytes, and recursive DAG.
2. Send exact subjects to task
   `019f8ab2-64ec-7922-a0f7-d5882133794e`.
3. Permit only listed read-only non-publishing checks once per snapshot.
4. Persist verdict and terminal iteration receipt create-once after the
   already-passed reservation and candidate readback.
5. On `REVISE`, return the repair list, preserve the iteration, add TDD, freeze
   a new snapshot, and create the next ordinal iteration.
6. Stop and use another namespace if a finding reaches installer-owned bytes,
   focused approval, runtime install, installed runtime, or environment
   contract.
7. Continue until one `APPROVE` iteration passes.
8. Render the final envelope in memory, create/read the separate finalization
   reservation first, publish/read the final envelope, recursively validate all
   iterations/residue, and close with a finalization terminal receipt.

Verification: one exact approved snapshot exists and production outputs remain
absent.

## S3.9: Publish and run one matrix

1. Render in memory the publication request, approved candidate identities,
   future output paths, and fixed receipt path; create/read the
   qualification-publication reservation before persisting the request or
   reading any parent.
2. Persist the request create-once, then validate roots, source, runtime,
   nine-pair environment, process/cache/TEMP
   cleanliness, predecessor immutability, approved review, and production
   absence.
3. Copy approved failure, runtime, and source candidate bytes create-once to
   fixed Successor-3 production paths.
4. Read back and recursively validate every output and DAG edge.
5. Create and independently verify one terminal publication receipt.
6. Freeze its request/reservation/receipt, three output identities, final source-review
   envelope, and DAG root as literal matrix parents.
7. Render the matrix contract in memory and create/read the matrix reservation
   before opening or rehashing any parent.
8. Invoke `run_attempt11_successor2_matrix.py` exactly once with the already
   reserved fixed identities and approved source declaration.
9. Do not run a dry production matrix.
10. Monitor only the original session.
11. Independently verify suite order, four-process arithmetic, raw streams,
     nested journals, results, passed receipt, source/runtime before/after, and
     zero cleanup state.

Verification: one passed publication receipt and one passed matrix receipt
exist; no predecessor or A0 mutation occurred.

## S3.10: Resume Attempt-11 diagnostic

1. Render the production binding in memory, create/read its reservation before
   parent reads, recursively validate parents, publish/read the binding, and
   close with a binding terminal receipt.
2. For each tooling-review ordinal, create/read the reservation first, persist
   subjects, obtain/persist the six-axis verdict, and close with a terminal
   receipt.
3. After one `APPROVE`, create/read a tooling-finalization reservation first,
   publish/read its final envelope, bind all residue, and close with a
   finalization terminal receipt.
4. Pass only the literal identities authorized by that finalization receipt to
   bootstrap; create/flush/same-handle-read the diagnostic reservation before
   opening or recursively validating any parent.
5. After reservation readback, recursively validate documents, fixed subjects,
   failure packet, runtime, source, publication, matrix, binding, and review.
6. Invoke the diagnostic child once with the frozen Attempt-10 validator and
   900000 ms timeout.
7. Independently verify the terminal diagnostic receipt.
8. Classify only from durable stdout/stderr, timing, exit/timeout, cleanup, and
   recursive DAG evidence.
9. Continue the approved Attempt-11 diagnosis/A0 plan.

Verification: the diagnostic supplies the next authoritative result; no
qualification infrastructure result is substituted for it.

## Checklist

- [ ] Successor-2 is frozen and never rerun.
- [ ] Successor-3 documents receive independent final approval.
- [ ] Document publication, focused iteration/finalization, source-review
  iteration/finalization, binding, and tooling-review iteration/finalization
  each have a passed reservation-bound terminal receipt.
- [ ] Nine-pair environment is exact and parent-independent.
- [ ] All new production and diagnostic paths are under `C:\a22or18-evi`.
- [ ] All seven C# sources compile in each required fresh process.
- [ ] Pre-install TDD is synthetic-only and remains immutable after approval.
- [ ] Post-install live evidence directly binds all predecessor residue and the
  frozen pre-install TDD identity.
- [ ] Real portable PowerShell startup creates no driver-relative cache.
- [ ] Runtime/source/TEMP/cache/process closure returns to zero drift.
- [ ] One passed source-review iteration receipt plus a passed finalization
  receipt and final envelope authorize publication.
- [ ] One qualification-publication receipt passes.
- [ ] Exactly one matrix invocation yields one passed terminal receipt.
- [ ] One diagnostic reservation/receipt is independently verified.
- [ ] No OrcaSlicer, A0 formal/claim/candidate/campaign/collector, or slicing
  action occurs before the diagnostic/A0 continuation gate.
