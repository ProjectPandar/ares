# Task 22O.70 implementation plan

1. Independently approve the pinned `PrintObject.cpp:3385-3386` and
   SurfaceCollection removal/append boundary plus exact private function seam.
2. Add ordinary production/test modules and preserve a failing behavioral RED.
3. Implement the minimal consuming stable-filter then borrowed copy-append
   operation, with trusted O67-then-O68-then-O69 composer input order.
4. Add focused kind/order/metadata/topology/empty/duplicate/ownership tests and
   compiling mutation audit with byte-exact restoration.
5. Run focused/dependency/workspace Nextest, strict Clippy/rustfmt, wasm32,
   x86_64/aarch64 Windows and macOS, and static repository gates.
6. Run independent read-only six-axis implementation review; repair only in the
   main thread and re-review to unconditional approval.

Outer traversal, second bridge pass, lifecycle, extrusion/motion/G-code/CLI and
golden parity remain deferred.

Implementation evidence: behavioral RED captured the missing Internal removal;
focused 3/3 and workspace 6,457/6,457 pass with two skipped; strict
Clippy/rustfmt, five portability checks, and 15/15 compiling mutations pass.
Independent six-axis review approved after the complete topology snapshot
repair, with no remaining item.
