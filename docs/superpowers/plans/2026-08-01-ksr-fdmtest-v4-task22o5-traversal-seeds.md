# Task 22O.5 traversal-seed plan

1. Add `classic::traversal::{types,classify}` for the source-cited `PerimeterGenerator.cpp:100-151,2537-2547` prefix.
2. Define an O5 successor nesting O4, aligned record/surface sidecars, ordered seed trees, source roles/routes, precise flow fields, unchanged source `f64` layer height, and pending branch provenance; defer the `ExtrusionEntity.hpp:164,170,551+` float narrowing to path construction.
3. Classify O4 roots iteratively and route only by source depth/smaller semantics; retain predecessor series by selector.
4. Build every sidecar before moving O4 ownership, preserving `None` and all source order.
5. Wire O5 after hierarchy and replace the terminal sink with iterative O5 consumption while keeping `ProjectSlicingIncomplete`.
6. Add direct classification/precision/deep-tree tests and in-memory KSR reachability, determinism, routing, preservation, and lifecycle tests.
7. Update architecture and roadmap ledgers.
8. Run focused Nextest, Clippy/check/WASM/format/diff and bounded LOC/forbidden scans. Full path construction and all post-line-151 behavior remain deferred as specified.
