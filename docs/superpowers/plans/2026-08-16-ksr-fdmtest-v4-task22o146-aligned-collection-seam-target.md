# Plan: Task 22O.146 aligned collection seam target

1. Tighten the focused KSR travel assertion to require the reference seam target and deferred lift Z; observe the existing mismatch.
2. Port source-style point-KD selection and the multi-region embedding gate used by aligned seam context.
3. Select the nearest acceptable seam across sibling perimeters in each collection, then emit XYZ only when the current travel consumes a new retract lift.
4. Run the focused inter-path and next-layer lift contracts, Clippy, rustfmt, and the KSR CLI slice; record the next divergence for Task 22O.147.
5. Commit and push this source-cited slice independently.
