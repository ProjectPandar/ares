# Plan: KSR FDM Test V4 task229 project print-flow ratio

1. Add a failing complete-slice assertion that changes only the 3MF `print_flow_ratio` option and requires proportionally scaled relative-E words.
2. Resolve the effective typed region ratio into motion options without fixture-specific defaults.
3. Port OrcaSlicer's effective-volume and filament-length arithmetic order for linear and fitted-arc paths.
4. Run the focused option-driven slice test and regenerate the unchanged baseline KSR fixture to record the next normalized divergence.
5. Run formatting and focused Clippy; commit and push this source-cited option slice independently.
