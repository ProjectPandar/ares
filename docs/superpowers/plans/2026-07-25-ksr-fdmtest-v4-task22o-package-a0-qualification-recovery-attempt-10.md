# Task 22O Package A0 Qualification Recovery Attempt 10 Plan

## Goal

Freeze both consumed Attempt-9 watchdog failures, measure the unchanged
generated-scope validator once under a finite extended bound, derive a
reproducible watchdog threshold from that measurement, rebuild all
current-attempt launch subjects in fresh A22OR15 roots, execute one fresh
71-role campaign, require strict collection, and publish only independently
approved sidecar bytes.

## Non-negotiable boundaries

- Never rerun either Attempt-9 watchdog or the Attempt-9 formal wrapper.
- Never create an Attempt-9 claim, candidate, retry, stage, or sidecar.
- Preserve both Attempt-9 scripts and watchdog transcripts byte-for-byte.
- Before document approval, do not run timing, edit A0 source, build, or launch.
- Run exactly one live extended timing measurement after document approval.
- Do not change validator source before successful timing and threshold
  derivation.
- The timing hard timeout is 900,000 ms.
- Derive `W` only as
  `ceil_to_60000(max(360000, 2 * M))`.
- Reject `W > 1,800,000`; do not silently increase the operational ceiling.
- Keep fixed Orca behavior, 71-role order, strict collector, and KSR records
  unchanged.
- Preserve all 122 existing A0 test IDs and 26 existing required IDs; derive
  and freeze the expanded counts after adding separate watchdog modules/tests.
- Keep every edited source/test file ASCII and below 400 physical lines.
- A timing, threshold derivation/recomputation/schema/write/review, formal,
  postclaim, or collector failure is terminal and requires a separately
  reviewed next attempt.
- Any sidecar revision/rejection/disagreement is terminal for Attempt 10.
- Preserve the governing Task 22O spec and plan hashes.
- After Package A0, keep Options 3MF-derived, forbid fixture/reference
  constants and legacy fallbacks, remove touched obsolete pinning tests, use
  real Rust/test modules below 400 LOC without splitting macros, and use the
  same final six-axis reviewer through every repair loop.

## A0R10.1: freeze Attempt-9 failure

1. Rehash the Attempt-9 specification, plan, final document/tooling evidence,
   source ledger, complete test transcript, build result, qualification
   inputs, prelaunch evidence, and launch approval.
2. Rehash both watchdog scripts against their exact frozen absolute paths,
   byte counts, and SHA-256 values.
3. Rehash the 20,014-byte Attempt-9 formal runner and its exact terminal
   source-ledger row.
4. Rehash the exact rooted v1/v2 transcript paths from the specification and
   parse each with a closed physical-line grammar.
5. Verify both failures occurred in non-formal mode at the exact 180-second
   boundary with empty stdout/stderr.
6. Verify Attempt-9 formal transcript, claim, candidate, and runner
   temporaries remain absent.
7. Verify no matching process or recursive Python cache remains.

Verification: two fresh-process read-only audits report identical hashes,
failure semantics, and absence facts with no filesystem mutation.

## A0R10.2: approve the document frame

1. Freeze exact ASCII/LF identities for this specification and plan.
2. Persist the rejected revision-1 reports and exact reviewed bytes under
   `C:\a22or15-evi\document-reviews\iteration-0001` create-once.
3. Allocate
   `C:\a22or15-evi\document-reviews\iteration-0002` create-once for revision 2.
4. Send identical revision-2 document bytes to the same distinct governance
   and technical reviewer identities.
5. Persist both reports and a canonical envelope binding exact documents,
   reviewer identities, verdicts, and measurement authorization.
6. If either reviewer returns `REVISE`, retain the iteration, repair only
   listed findings, and allocate the next consecutive iteration.
7. Publish a hash-chained registry and final document envelope only after both
   reviewers approve the same latest bytes.

Verification: reviewers differ; final subjects equal repository documents;
all iterations are immutable; `measurement_authorized=true`; A0 is unchanged.

## A0R10.3: TDD the external timing runner

1. Write a synthetic child fixture that can succeed, sleep, exit nonzero,
   emit stderr, and create prohibited residue.
2. Write focused tests first for successful receipt grammar, timeout
   recursive kill/wait, nonzero exit, stderr rejection, create-once collision,
   environment clearing, forbidden-path detection, and cleanup failure.
3. Run the tests and retain the authentic RED before the production runner
   exists.
4. Implement the minimum PowerShell runner below 400 LOC.
5. Turn every focused test GREEN without invoking the live validator.
6. Verify PowerShell AST, ASCII/LF, final newline, bounded timeout, exact child
   argv, and no formal-runner reference.
7. Freeze a source ledger and focused RED/GREEN results under
   `C:\a22or15-evi`.
8. Send exact runner/tests/evidence to an independent read-only reviewer.

Verification: independent review approves with no findings; production timing
receipt remains absent; A0 and repository bytes remain unchanged.

## A0R10.4: run one finite timing measurement

1. Rehash all runner-bound executables, documents, Attempt-9 launch subjects,
   scripts, and failure transcripts.
2. Require no relevant process/cache, claim/candidate, formal transcript, or
   runner temporary.
3. Start bundled Python with `-B`, exact approval script, exact roots/tooling
   directory, bytecode disabled, and PATH limited to bound PowerShell/Git.
4. Wait at most 900,000 ms.
5. On timeout, recursively kill/wait, capture failure, verify cleanup, publish
   the create-once receipt, and stop for a separately reviewed next attempt.
6. On completion, require exit zero, empty stderr, exact sorted 155-path JSON,
   unchanged launch subjects, and clean post-state.
7. Publish `C:\a22or15-evi\timing\measurement-v1.json` as canonical JSON with
   create-new semantics and forced flush.
8. Independently rehash and review the receipt and post-state.

Verification: one successful receipt supplies `M`, path count 155, exact
stdout hash, all absence attestations, and no filesystem mutation except the
external receipt.

## A0R10.5: derive and approve the watchdog threshold

1. Parse `M` from the successful timing receipt.
2. Independently compute
   `W = ceil_to_60000(max(360000, 2 * M))` in Python and PowerShell.
3. Require identical results and `M < W <= 1,800,000`.
4. Bind multiplier 2, minimum 360,000 ms, quantum 60,000 ms, maximum
   1,800,000 ms, inputs, result, timing runner/receipt, stdout hash,
   Attempt-9 launch, and document final in a canonical create-once envelope.
5. Validate the exact canonical schema and publish
   `C:\a22or15-evi\timing\threshold-v1.json` create-once.
6. Obtain independent read-only approval of the exact threshold envelope.
7. Treat any parse, formula, bounds, dual-recomputation, schema, create-new
   write, or review failure as terminal for Attempt 10; retain the
   non-authorizing evidence and require Attempt 11.

Verification: both implementations produce the same `W`; no manually selected
replacement threshold exists.

## A0R10.6: publish the prior-attempt handoff

1. Before mutating the live runner, copy the exact Attempt-9
   `tooling/run_fixed_qualification.ps1` create-once to
   `coverage-repair/tooling-review/attempt-10/prior-attempt/attempt9-runner.ps1`.
2. Bind that rooted 20,014-byte identity and the exact terminal Attempt-9
   source-ledger row.
3. Create an Attempt-10 handoff that binds both Attempt-9 watchdog scripts and
   transcripts, the immutable runner copy, the unused Attempt-9 launch
   approval, timing evidence, and all absence facts.
4. Bind the unchanged Attempt-9 prior-attempt handoff transitively so the
   Attempt-7 and Attempt-8 candidates remain frozen.
5. Capture a complete no-follow snapshot of all bound current history without
   following reparses.
6. Require logical retained order
   `["8a5aab7461b877b1","11d07a6fd071c211"]` and equal sorted physical set.
7. Reject any changed root/path pair, byte, order, alias, extra child, or
   invented Attempt-9 formal result.

Verification: handoff subjects are create-once, nested identities rehash, and
no claim/candidate/stage is created.

## A0R10.7: adapt current-attempt tooling

1. Create the new exact live wrapper
   `tooling/run_attempt10_fixed_qualification.ps1`, bind it as the launch
   `powershell` subject, and remove the old live
   `tooling/run_fixed_qualification.ps1` only after its history copy validates.
2. Change only current-attempt domains, paths, kinds, attempt numbers, runner
   messages, review/launch/claim/publication paths, and formal transcript to
   Attempt 10.
3. Preserve every historical Attempt-7, Attempt-8, and Attempt-9 literal and
   identity.
4. Validate the Attempt-10 handoff and exact two-child retained history.
5. Add `timing_evidence` to the launch subjects and require exactly the twelve
   keys named by the specification.
6. Add small separate `a0_watchdog_contract.py` and
   `tests/test_a0_watchdog_contract.py` modules for the threshold table,
   rounding boundaries, maximum, receipt mutations, one-millisecond launch
   mismatch, new handoff, frozen old-runner substitution, and Attempt-9
   no-retry contract. Preserve every existing test ID.
7. Bind `generated_scope_timeout_ms=W` in the launch contract. Pass it
   explicitly to only the formal runner's preclaim generated-scope call and
   retain 180,000 ms for the later build/input hydration call. Test both call
   sites and reject omission, old/history-runner execution, path substitution,
   or fallback to the default.
8. Independently enumerate final static A0 paths, exact repository documents,
   and the sorted Attempt-10 generated-scope paths. Freeze derived topology
   counts, current generated-scope count, and normalized compact-JSON SHA-256
   in the static-topology subject. Do not reuse the Attempt-9 155-path/hash
   values.
9. Regenerate publication policy from final source semantics.
10. Derive current document iteration from the Attempt-10 registry.

Verification: focused tests pass; every original test ID remains and the new
exact discovered set matches topology; no stale current-attempt Attempt-9
domain/path/kind remains; historical validators still use historical literals.

## A0R10.8: run complete tooling verification

1. Run the complete newly discovered test set with bundled Python, `-B`, and
   bytecode disabled.
2. Run Python AST, PowerShell AST, ASCII, LOC, role-count, test-count,
   required-ID, forbidden-macro, topology, policy, threshold, and transcript
   checks.
3. Require `0 < LOC < 400` for every source/test file.
4. Rehash both retained candidate trees before and after.
5. Revalidate all prior-attempt nesting and no process/cache residue.

Verification: one complete create-once result reports every discovered test
passing and every static marker after final Attempt-10 adaptation.

## A0R10.9: prepare fresh A22OR15 build and inputs

1. Allocate new pairwise tree-disjoint source, build, install, and evidence
   roots that do not reuse A22OR8-A22OR14 or Attempt-6 roots.
2. Recreate the fixed-source derivative from commit/tree and patch identities.
3. Configure, build, install, and run fixed CTest RED/GREEN.
4. Regenerate all four adjacent-DLL runtime closures and probe identity.
5. Emit canonical build-result v2 and deterministic qualification-inputs v2
   using the final Attempt-10 candidate domain.
6. Require Python and PowerShell candidate IDs to agree.
7. Verify candidate absence and exact two-child prelaunch parent state.
8. Obtain independent read-only build/input approval.

Verification: production validation passes twice in fresh processes; all roots
are literal, canonical, non-aliased, and disjoint.

## A0R10.10: tooling review and launch approval

1. Freeze final topology, policy, source ledger, complete tests, timing and
   threshold evidence, prior-attempt handoff, and approved build/input
   evidence.
2. Send exact subjects to a six-axis reviewer distinct from both document
   reviewers.
3. For findings, retain the iteration, repair only the fix list, repeat
   invalidated checks/builds, and re-review in a new create-once iteration.
4. On six-axis approval, publish the tooling final, prelaunch evidence, and
   sole Attempt-10 launch envelope create-once.
5. Require exactly twelve launch subjects, one authorized candidate, absent
   claim/candidate/formal transcript, exact retained parent, and clean state.
6. Require the launch timeout to equal the independently recomputed threshold
   and require Python and PowerShell runner validation to agree.

Verification: full approval reports Attempt 10, unchanged reviewed subjects,
reviewer independence, and formal execution authorized.

## A0R10.11: derived watchdog and one atomic campaign

1. Run live Attempt-10 `generated-scope` exactly once under derived `W`.
2. Reuse the timing measurement's executable/environment, recursive cleanup,
   receipt schema, and absence contracts, but write only the distinct
   create-once watchdog receipt
   `C:\a22or15-evi\watchdog\attempt10-generated-scope-v1.json`; never reuse or
   overwrite `measurement-v1.json`.
3. Require the path count, sorted output, and normalized hash to equal the
   current Attempt-10 contract frozen in launch-bound static topology; the
   Attempt-9 measurement output is timing evidence only.
4. Require the watchdog receipt to bind the exact launch approval and
   static-topology identities.
5. Rehash launch subjects and retained candidates immediately before formal
   invocation.
6. Require claim, candidate, formal transcript, and runner temporaries absent.
7. Invoke exact
   `tooling/run_attempt10_fixed_qualification.ps1` once; its preclaim
   generated-scope call uses the bound `W`, while hydration remains at
   180,000 ms.
8. Execute ordinals 1-71 without resume, retry, overwrite, selection, or old
   leaf reuse.
9. Require postclaim logical order to append exactly the Attempt-10 candidate
   and physical membership to equal its sorted set.

Verification: watchdog passes; campaign reports 71 passed processes, 69 CLI,
two direct, paired equality, no retry/selection, and no residue. Any failure
requires Attempt 11.

## A0R10.12: strict postcampaign collection

1. In a fresh process validate launched generated scope.
2. Invoke the existing strict collector exactly once over only the fresh
   candidate.
3. Validate all leaves and the closed evidence set before stage creation.
4. Require standalone/embedded datadir equality in all 69 CLI roles.
5. Require both direct roles to omit datadir evidence.
6. Require complete ordered corpus and byte-identical candidate tree before
   and after collection.

Verification: collector returns the full corpus with no evidence error.
Rejection consumes Attempt 10.

## A0R10.13: stage, review, and publish

1. Validate semantic coverage and workspace integrity.
2. Allocate one create-once publication iteration.
3. Build corpus bytes twice in memory and publish only byte-identical output to
   the stage.
4. Assemble and validate the staged manifest.
5. Send identical stage subjects to two independent sidecar reviewers
   distinct from the tooling reviewer.
6. Any revision, rejection, disagreement, or subject drift retains evidence,
   consumes Attempt 10, and requires Attempt 11.
7. Only dual approval publishes final corpus, manifest, reports, registry, and
   final sidecar create-once.
8. Make the stage unavailable and run deep approved verification.

Verification: final bytes equal one approved stage and resolve only relative
to the explicit final root.

## A0R10.14: release and resume Task 22O

Only after deep approved verification:

1. update the Task 22O progress ledger with exact identities and results;
2. release the separately reviewed adapter/engine-selection package;
3. continue tracked Rust Task 22O implementation one Option slice at a time;
4. derive every Option solely from the supplied 3MF;
5. remove touched obsolete Orca source-pinning tests;
6. keep source and separate test modules below 400 LOC without `include!` or
   `include_bytes!` splitting;
7. run exact Ares KSR G-code verification;
8. send the exact tracked diff and fresh execution evidence to one reviewer
   independent of the implementing main thread for all six required axes; and
9. repair the ordered finding list and return to the same reviewer until every
   axis approves or a precise blocker is recorded.

## Checklist

- [ ] Attempt-9 failures and absent formal state are frozen.
- [ ] Two document reviewers approved identical Attempt-10 spec/plan bytes.
- [ ] External timing runner passed focused TDD and independent review.
- [ ] One unchanged-validator timing run completed under 900,000 ms.
- [ ] Derived threshold `W` passed dual recomputation and independent review.
- [ ] Attempt-10 prior handoff and current-attempt adaptation passed.
- [ ] All original and newly discovered tests plus static checks passed.
- [ ] Fresh A22OR15 build/input evidence passed independent review.
- [ ] Six-axis tooling reviewer approved exact launch subjects.
- [ ] Derived watchdog and one campaign passed.
- [ ] Strict collector passed all 69 CLI and two direct leaves.
- [ ] Two sidecar reviewers approved identical stage bytes.
- [ ] Deep verification passed and Package A0 released.
- [ ] Final Rust six-axis review loop reached approval or a precise blocker.
