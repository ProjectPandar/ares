# Task 22O Package A0 Tooling Review Attempt 5 Recovery Plan

## Objective

Recover from the consumed Attempt-4 evidence-publication order without changing
slicing behavior or adding a tooling source or test module. Preserve the exact
Attempt-4 ledger, completed transcript, independent rejection, and rejected
envelope; obtain an approved Attempt-5 document gate; version existing public
contracts to Attempt 5; finish all tests and static gates; build two identical
unpublished ledgers; only then publish fresh Attempt-5 evidence and return it
to the same reviewer.

Formal Orca remains prohibited until complete Attempt-5 approval and all
post-review gates pass on unchanged subjects.

## Working rules

- Treat fixed Orca, Package 0, the 71-role order, all prior repair documents,
  and all 16 prior rejection-history subjects as immutable.
- Substitute the exact final Attempt-4 report and envelope identities into the
  specification before creating the Attempt-5 document pair.
- Use fresh Attempt-5 document-gate and final-evidence paths.
- Add no source module, test module, required test ID, tracked Rust source,
  Cargo change, architecture change, roadmap feature, workflow change, or
  slicing behavior.
- Keep the source/test counts exactly 60/41/7/24/107/11 and prerequisites 31.
- Keep every source ASCII and below 400 physical lines.
- Use existing public validation, runner, assembly, and verification seams.
- Record versioning REDs before production versioning and make each slice green.
- Keep all four Attempt-5 final evidence paths absent until the full suite,
  CTest, static gates, mirror validation, and both unpublished ledger builds
  pass.
- Stage proposed evidence outside the repository and A0 closed set.
- Never touch `main.obj`; never use `include!` or `include_bytes!` for Rust
  source splitting.

## A0R5.1: finish and freeze Attempt-4 rejection history

1. Let the already-started Attempt-4 full suite finish without a source edit.
2. Confirm all 107 tests passed in 2,328.553 seconds.
3. Confirm fixed CTest passed 1/1 in 0.42 seconds and 0.44 seconds total.
4. Confirm the static result is 60 sources, 41 Python, seven PowerShell, and 31
   frozen prerequisites.
5. Preserve the existing 6,197-byte Attempt-4 source ledger with SHA-256
   `890b107e482ecccd3734b231e2f90c20c14ee9e40a6e98c4e0a39d4013151522`.
6. Preserve the 19,824-byte Attempt-4 transcript with SHA-256
   `9f0d52c35e42668db56295481dacb218f1d488c0afda407c5d85fd6abcb7a014`.
7. Persist the same reviewer's complete rejection response verbatim at the
   Attempt-4 final review path.
8. Create the canonical rejected Attempt-4 envelope from that response without
   changing the review text.
9. Require blocker `premature-final-source-ledger-publication`, attempt 4,
   rejected state, false formal authorization, no subject mutation, and reject
   verdict.
10. Compute and freeze the exact final review and envelope identities.
11. Rehash all 16 prior subjects and confirm every earlier identity remains
    exact.
12. Confirm no Attempt-5 document or final-evidence path exists.

Verification: Attempt 4 is complete rejected history; it is not repaired in
place and no formal Orca process exists.

## A0R5.2: create and approve the Attempt-5 document gate

1. Replace the two not-yet-known Attempt-4 final identities in the proposed
   specification with the exact frozen values.
2. Create the Attempt-5 specification and plan as ASCII/LF regular files with
   one final newline and fewer than 400 physical lines.
3. Confirm both documents retain the fixed Orca tag, commit, tree,
   `PerimeterGenerator.cpp`, `process_classic()` lines 1144-1692, and fixed
   Voronoi boundary.
4. Confirm the documents specify minimal recovery only and no new source or
   test module.
5. Confirm the four Attempt-5 final evidence paths are absent.
6. Send the exact document hashes and all 16 immutable prior identities to
   `/root/task22o_a0r_tooling_six_axis_review`.
7. Persist the complete document response create-once at
   `coverage-repair/tooling-review/attempt-5/document-review.md`.
8. Persist the canonical 13-key document envelope create-once at
   `coverage-repair/tooling-review/attempt-5/document-approval-envelope.json`.
9. Require the exact two document and five check approvals, sole terminal
   `VERDICT: APPROVE`, true implementation authorization, false formal Orca
   authorization, and no subject mutation.
10. Rehash the 16 prior subjects plus the Attempt-5 spec, plan, review, and
    envelope.

Verification: exactly 20 document-gate subjects are bound and unchanged.
Source or test edits begin only after document approval.

## A0R5.3: RED the Attempt-5 public versioning contract

Update existing tests and fixtures before production:

1. point current ledger, transcript, report, and final envelope expectations to
   `coverage-repair/tooling-review/attempt-5/`;
2. require Attempt-4 final evidence as immutable rejected history;
3. require the Attempt-5 document envelope in current approval subjects;
4. require attempt 5 and the five new ordered repair IDs;
5. require the ten-key final subject object;
6. require `active_review_attempt=5`;
7. require the four new document fields and five new tooling fields;
8. require closed-set counts 13/104/10/7 and tooling-review prefix 28;
9. require the exact Attempt-5 runner approval summary;
10. retain source roles 60, Python 41, PowerShell seven, test roles 24,
    discovery 107, required tests 11, and prerequisites 31.

Use existing public tests in `test_tooling_approval`, runner contract,
manifest, assembly, integration, and deep-verification modules. Add no new test
module or required ID.

Run focused tests and record only clean failures caused by current Attempt-4
production paths, attempt values, report/envelope semantics, group bindings, or
runner summary. A stale runner source pin or broken fixture setup is not the
behavioral RED.

Verification: the public tests demonstrate the exact Attempt-5 versioning gap.

## A0R5.4: GREEN frozen history and approval semantics

1. Version current approval paths to the Attempt-5 ledger, transcript, report,
   and envelope.
2. Add the approved Attempt-5 document-gate identity.
3. Extend strict history validation with all eight Attempt-4 subjects.
4. Validate the Attempt-4 report by its exact immutable identity.
5. Validate the Attempt-4 envelope as canonical, attempt 4, rejected, blocked
   by `premature-final-source-ledger-publication`, false formal authorization,
   and no subject mutation.
6. Do not reuse the current Attempt-5 approval parser to reinterpret Attempt-4
   rejection.
7. Version the report header, five repair IDs, attempt, final envelope paths,
   ten subject keys, and approval summary to Attempt 5.
8. Keep the 31 prerequisite and 12 nested-identity no-follow gates unchanged.
9. Keep pre-review and full approval at their existing public seams.
10. Run focused approval history, transcript, prerequisite, and envelope tests
    green.

Keep `a0_tooling_approval.py` and `a0_tooling_evidence.py` below 400 lines by
replacing versioned definitions and sharing existing closed-schema helpers.
Do not add a compatibility re-export or fallback.

## A0R5.5: GREEN manifest, assembly, and deep contracts

1. Extend `REPAIR_DOCUMENT_PATHS` with the Attempt-5 plan and specification.
2. Extend repair additions with exactly the six Attempt-5 A0 artifacts.
3. Require exact closed-set counts:
   - parent subjects 13;
   - repair additions 104;
   - repository documents 10;
   - excluded residue seven;
   - tooling-review prefix 28.
4. Retain every previous document and tooling role.
5. Add, in order:
   - `tooling_attempt_5_specification_role`;
   - `tooling_attempt_5_plan_role`;
   - `tooling_attempt_5_document_review_role`;
   - `tooling_attempt_5_document_approval_envelope_role`.
6. Add, in order:
   - `attempt_5_test_result_role`;
   - `attempt_5_source_ledger_role`;
   - `attempt_5_review_report_role`;
   - `attempt_5_review_envelope_role`;
   - `attempt_5_fixed_probe_evidence_roles`.
7. Bind the fixed-probe list to the unchanged seven evidence roles.
8. Set `active_review_attempt=5`.
9. Retain `source_roles=60` and `test_roles=24`.
10. Update existing manifest fixture, manifest tests, assembly tests, integration
    tests, and deep mutation tests without adding a test module.
11. Run focused manifest, assembly, subject-verification, and deep tests green.

Verification: all eight new artifact-source bindings are classified, ordered,
and reachable, with no unclassified path or stale Attempt-4 active binding.

## A0R5.6: finalize runner pins and source bytes

1. Finish every Attempt-5 edit to both runner-pinned Python sources.
2. Derive both pinned paths as siblings of the tooling directory.
3. Compute exact bytes and SHA-256 for the final approval and evidence sources.
4. Update the existing two-entry runner preflight table.
5. Switch the expected stdout to the exact Attempt-5 approval summary.
6. Require both source identity checks before Python process construction,
   qualification-input access, candidate creation, or child launch.
7. Run the existing independent two-source mutation test green.
8. Run the existing A0 and repository runner-topology representatives green.
9. Require no sentinel, input access, candidate, filesystem mutation, Orca,
   fixed-probe, or `run_one` process on rejection.
10. Make no later edit to either pinned Python source or the runner.

Verification: both final source pins reject independently at the public runner
boundary and every source remains below 400 physical lines.

## A0R5.7: focused and static preflight with final paths absent

1. Confirm all four Attempt-5 final evidence paths are absent.
2. Build the source set from the unchanged 60 sorted unique paths.
3. Require exactly 41 Python, seven PowerShell, and 24 test-role paths.
4. Parse every Python source with the bundled Python AST.
5. Parse every PowerShell source and reject literal `$args`.
6. Require ASCII and one to 399 physical lines for all 60 sources.
7. Scan only Rust-suffix sources for forbidden `include!` and
   `include_bytes!`; Attempt 5 adds no Rust source.
8. Discover exactly 107 unique tests.
9. Require all 11 required IDs exactly once.
10. Run every affected focused approval, runner, manifest, assembly, and deep
    module.
11. Revalidate all 31 prerequisites and 12 nested identities.
12. Confirm no formal Orca process and no closed-set staging residue.

Verification: focused behavior and all static gates pass while final evidence
remains absent.

## A0R5.8: complete unpublished final qualification

Use a unique OS-temporary staging directory outside the repository and A0
closed set.

1. Confirm no Orca, formal runner, `run_one`, or fixed-probe process is active.
2. Confirm all four Attempt-5 final paths are absent.
3. Run bundled-Python discovery and all 107 tests.
4. Require 107/107 pass with no failure, error, skip, or expected failure.
5. Run fixed CTest once and require exactly 1/1.
6. Run the complete 60/41/7/31 static and frozen gates.
7. Recheck exact 13/104/10/7 closed-set arithmetic and tooling prefix 28.
8. Build unpublished ledger A from fresh reads of all 60 final sources.
9. Assemble the proposed Attempt-5 transcript against ledger A with:
   - attempt 5;
   - `FORMAL_ORCA_EXECUTED=false`;
   - exact closed unittest and CTest grammar;
   - all 107 IDs once with `ok`;
   - `PYTHON_AST_PASS=41`;
   - `POWERSHELL_AST_PASS=7`;
   - `ASCII_AND_LOC_LT_400_PASS=60`;
   - `FROZEN_PREREQUISITES_PASS=31`.
10. Copy proposed ledger and transcript into a fresh non-reparse evidence
    mirror, never into final production destinations.
11. Run production `validate_pre_review()` against the mirror.
12. Build unpublished ledger B from independent fresh reads immediately before
    publication.
13. Require ledger A and ledger B byte-identical.
14. Require every current source byte/hash to match both ledgers.
15. Rehash all 20 immutable document-gate subjects.
16. Reconfirm all four Attempt-5 final paths absent.

If any source changes or any check fails, discard staging and restart this
entire step. Do not reuse a pre-change test run, transcript, or ledger.

Verification: the complete suite, CTest, static gates, mirror validation, and
two identical unpublished source builds all pass before publication.

## A0R5.9: publish the ledger and transcript create-once

1. Hold the final source set read-only for the remainder of Attempt 5.
2. Precheck both ledger and transcript destinations absent.
3. Publish ledger A at
   `coverage-repair/tooling-review/attempt-5/source-files.sha256` using
   create-new semantics.
4. Publish the validated transcript at
   `coverage-repair/tooling-review/attempt-5/mock-test-results.txt` using
   create-new semantics.
5. Read both files back and require exact staged bytes and hashes.
6. Run production `validate_pre_review()` against the real A0 and repository.
7. Require source count 60 and test count 107.
8. Rehash every current source and require the published ledger still exact.
9. Confirm the Attempt-5 final review and envelope paths remain absent.
10. Confirm no formal Orca process started.

Verification: publication occurs only after A0R5.8 and neither destination is
overwritten or mutated.

## A0R5.10: same-reviewer six-axis review

Send the exact Attempt-5 ledger and transcript identities, all 20 immutable
document-gate identities, the Attempt-4 rejected history, and the five required
repair IDs to `/root/task22o_a0r_tooling_six_axis_review`.

Require read-only review of:

1. requirements completeness;
2. logical correctness;
3. boundary cases;
4. code quality;
5. test coverage;
6. actual execution results.

For approval, require:

- the exact five-line Attempt-5 report header;
- six ordered approved axis lines;
- five ordered resolved repair lines;
- no blocker;
- sole final `VERDICT: APPROVE`;
- a canonical 13-key final envelope;
- exactly ten subject keys;
- attempt 5 and approved state;
- all five repairs required and resolved;
- no subject mutation;
- true formal Orca authorization.

Persist the complete review and canonical envelope create-once at:

```text
coverage-repair/tooling-review/attempt-5/six-axis-review.md
coverage-repair/tooling-review/attempt-5/review-envelope.json
```

If the reviewer rejects, preserve all Attempt-5 paths unchanged, derive axes,
blockers, and the resolved repair subset exactly from the response, publish the
canonical rejected envelope, and require Attempt 6 before source edits.

## A0R5.11: unchanged-subject approval and release gate

After an approved report and envelope:

1. run production full approval;
2. require the exact Attempt-5 approval summary;
3. run the formal-runner no-launch preflight;
4. run assembly;
5. run subject verification;
6. run deep verification;
7. rehash ledger, transcript, report, envelope, document gate, all prior
   history, both runner-pinned sources, and all fixed evidence;
8. require no subject mutation;
9. confirm `runs/qualified` is empty before formal qualification.

Only after every check passes may the detached Orca build and 71 formal
qualification processes begin.

## Completion checklist

- [ ] Attempt-4 rejected history has eight exact subjects and the exact blocker.
- [ ] All 16 prior subjects remain byte-identical.
- [ ] Attempt-5 document gate is independently approved before source edits.
- [ ] No new source module, test module, required ID, Rust source, or slicing
      behavior exists.
- [ ] Counts remain exactly 60/41/7/24/107/11/31.
- [ ] Closed-set arithmetic is exactly 13/104/10/7 with tooling prefix 28.
- [ ] Active review attempt is 5.
- [ ] Both runner-pinned source identities are final before the full run.
- [ ] All four Attempt-5 final paths remain absent through focused verification.
- [ ] Full unittest, fixed CTest, and static gates pass.
- [ ] Two independent unpublished ledgers are byte-identical.
- [ ] Ledger and transcript are published only after those gates.
- [ ] The same reviewer approves all six axes and five repairs.
- [ ] Full approval, runner preflight, assembly, subject verification, and deep
      verification pass on unchanged subjects.
- [ ] No formal Orca execution occurs before complete approval.
