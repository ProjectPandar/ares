# Task 22O Package A0 Qualification Recovery Attempt 8 Plan

## Goal

Freeze the consumed Attempt-7 preclaim timeout, repair repeated whole-tree
hashing test-first, rebuild every launch-bound subject in fresh roots, execute
one new 71-role qualification campaign, and publish only independently approved
sidecar bytes.

## Non-negotiable boundaries

- Never rerun or overwrite the Attempt-7 wrapper, runner, transcript, review
  evidence, launch envelope, or stale A22OR11 build/input evidence.
- Never create an Attempt-7 claim or candidate.
- Do not alter failed candidate `8a5aab7461b877b1` or any child.
- Do not change fixed Orca derivative behavior, the 71-role order, or expected
  KSR records.
- Do not solve the timeout by increasing 180 seconds or using a persistent
  cache.
- Each public validation call must independently snapshot the live tree;
  failed-candidate content reuse is scoped to that call only.
- Before document approval, only create-once document-review iteration
  artifacts are authorized; no other evidence, source edit, or fresh build is.
- Keep all edited source/test files ASCII and below 400 physical lines.
- A formal Attempt-8 failure is terminal and requires Attempt 9.
- Preserve the governing Task 22O specification identity
  `78c44972e284eb615bf96228cbc5d0fe3a5c731a853c3b1cf518f92219b95674`
  and plan identity
  `94c361d0d4c89eb5019f07f3a3e4101b8d89857d02c06629e3c794920f645e80`.
- After Package A0, keep all Options 3MF-derived, forbid fixture/reference
  constants and legacy fallbacks, remove touched obsolete pinning tests, use
  real Rust/test modules below 400 LOC without source-splitting macros, and use
  the same final six-axis reviewer through every repair loop.

## A0R8.1: read-only Attempt-7 preflight

1. Rehash the 234-byte formal transcript and all ten Attempt-7 launch subjects.
2. Verify the exact timeout text and wrapper exit code 1.
3. Verify the Attempt-7 claim and candidate are absent.
4. Verify the qualified parent contains only `8a5aab7461b877b1`.
5. Verify no matching formal runner, validator, Orca, or fixed-probe process
   remains.

Verification: two fresh-process validations produce identical facts and no
filesystem mutation.

## A0R8.2: approve the document frame

1. Reconstruct and hash-verify exact revision-1 bytes and persist them with
   their two initial `REVISE` reports in create-once iteration `0001`.
2. Reconstruct and hash-verify exact revision-2 bytes and persist them with
   their two second `REVISE` reports in create-once iteration `0002`.
3. Reconstruct and hash-verify exact revision-3 bytes and persist them with
   governance `REVISE` and technical `APPROVE` reports in iteration `0003`.
4. Reconstruct and hash-verify exact revision-4 bytes and persist them with
   governance `APPROVE` and technical `REVISE` reports in iteration `0004`.
5. Reconstruct and hash-verify exact revision-5 bytes and persist them with
   governance `REVISE` and technical `APPROVE` reports in iteration `0005`.
6. Freeze this revision-6 specification and plan identity.
7. Allocate `attempt-8/document-reviews/iteration-0006`.
8. Send identical revision-6 bytes to the same distinct governance and
   technical reviewers.
9. Persist both reports and a canonical review envelope.
10. If either returns `REVISE`, repair only listed findings and allocate the next
   consecutive create-once iteration.
11. Publish a hash-chained registry and final document envelope only when both
    reviewers return `APPROVE`.

Verification: all six five-file iterations are immutable and hash-chained;
reviewer identities differ; the terminal reports bind the same exact document
bytes; and `implementation_authorized=true`.

## A0R8.3: publish the prior-attempt handoff

1. Copy the exact pre-failure runner bytes create-once to
   `attempt-8/prior-attempt/attempt7-runner.ps1`.
2. Record independent rooted copy identity `{root, path, bytes, sha256}` and
   require its 19,865-byte
   `301ef651ebf5a1b0c296c8815dfbedbdeb1a31d14b7f50c75c70389e45f86ba4`
   content identity. Separately, uniquely select terminal Attempt-7 ledger row
   `tooling/run_fixed_qualification.ps1` and require the same SHA-256 plus
   exactly 390 physical lines; do not compare the different identity schemas.
3. After document approval, immediately recapture the matching formal runner,
   validator, Orca, and fixed-probe process set and require it to be empty.
4. Create canonical `attempt-8/prior-attempt-v1.json` with the transcript,
   runner copy, source ledger, Attempt-7 launch envelope, exact ten nested
   launch subjects, exit/error/timestamp facts, absence facts, parent children,
   and empty process set.
5. Rehash every bound file and recursively validate the exact ten-subject set
   twice in fresh processes.
6. Record the handoff identity for tooling and launch review.

Verification: both subjects are create-once, the live Attempt-7 runner is still
unchanged, and no claim/candidate/process is created.

## A0R8.4: write the focused REDs

1. Extend an existing tooling-approval integration test with a delegating spy
   around the real file-hash seam.
2. Call public `validate_generated_scope_and_binding`,
   `validate_generated_scope`, `validate_full_approval`, and
   `validate_pre_review` on small real approval fixtures; cover
   `validate_launch_binding` through the shared generated-scope implementation.
3. Assert retained failed-candidate content is physically hashed no more than
   once in each top-level call.
4. Call each validator again and assert a second independent scan occurs.
5. Mutate one retained file's content between calls and assert the second call
   rejects it. Do not add a new same-byte hardlink-topology contract.
6. Extend the existing formal-runner preflight test to AST-extract the real
   `Invoke-A0Python`, inject a short deadline, and assert exact timeout,
   recursive child termination, unchanged filesystem, and absent
   claim/candidate.
7. Run only those existing test IDs and retain the failing output.

Verification: the tests fail for the reproduced reasons on pre-repair sources,
without invoking the formal wrapper or Orca.

## A0R8.5: implement one per-call snapshot

1. Add an optional precomputed-row input to failed-candidate snapshot
   validation without changing default standalone behavior.
2. At each public approval entry, take one no-follow A0 tree snapshot.
3. Derive candidate-relative rows from the exact
   `runs/qualified/8a5aab7461b877b1/` prefix.
4. Pass the same rows through tooling iterations, transcript invariant scope,
   review-ready closed set, prelaunch closed set, and failed-tree validation.
5. Keep the rows local to the synchronous call; do not retain them globally.
6. Keep the production timeout at 180000 ms. Add only the defaulted internal
   timeout parameter needed by the focused test.
7. In the same implementation phase, complete every current-attempt source
   adaptation: Python/PowerShell candidate domain; Attempt-8 paths, kinds,
   numbers, runner errors, literal launch/claim paths; prior-attempt validator;
   eleven-subject launch validator; and fixture expectations. Historical
   Attempt-7 paths and kinds remain unchanged.
8. Regenerate the Attempt-8 publication policy and static topology with the
   same 69 source paths, 28 test paths, 122 test IDs, and 18 exact repository
   documents: the prior 14 plus the Attempt-8 pair and inherited Task 22O pair.

Verification: focused REDs turn GREEN; two calls rescan; between-call mutation
still fails; every current-attempt source adaptation is present; and all touched
files remain below 400 LOC.

## A0R8.6: run complete tooling verification

1. Run all 122 unit tests with bundled Python and bytecode disabled.
2. Run Python AST, PowerShell AST, ASCII, LOC, source-role, test-role, forbidden
   macro, and static-topology checks.
3. Rehash the failed candidate before and after; require byte-identical
   snapshots.
4. Verify the Attempt-7 evidence and prior-attempt handoff remain unchanged.
5. Verify no process or cache residue remains.

Verification: one complete transcript reports 122/122 and all static markers
after all Attempt-8 source/domain/path/kind changes.

## A0R8.7: prepare fresh build and inputs

1. Allocate four new pairwise tree-disjoint source/build/install/evidence roots.
2. Recreate the fixed source derivative from commit/tree and patch identities.
3. Configure, build, install, and run the fixed CTest RED/GREEN grammar.
4. Regenerate all four adjacent runtime-closure records and probe identity.
5. Emit canonical build-result v2 and deterministic qualification inputs v2
   using the already verified Attempt-8 candidate domain.
6. Independently require Python and PowerShell candidate IDs to agree.
7. Verify the new candidate is absent and the parent has only failed history.

Verification: production build-result validation passes in two fresh processes;
all roots are literal, canonical, non-aliased, and disjoint from A22OR8-A22OR11
and Attempt-6 roots.

## A0R8.8: adapt and review Attempt-8 launch evidence

1. Revalidate that all current-attempt kinds and paths use
   `coverage-repair/tooling-review/attempt-8` while historical Attempt-7 paths
   and kinds remain unchanged.
2. Freeze the already verified static topology with the same 69 source paths,
   28 test paths, 122 test IDs, and 18 repository document paths.
3. Bind the already published Attempt-8 publication policy and approved
   prior-attempt handoff.
4. Freeze the final source ledger and complete test/build/input transcript.
5. Allocate a create-once tooling-review iteration.
6. Send exact subjects to an independent six-axis reviewer covering
   requirements completeness, logical correctness, boundary cases, code
   quality, test coverage, and actual execution results.
7. Require that tooling reviewer identity to differ from both document
   reviewers.
8. Apply findings only through a new source/transcript/review iteration and,
   before launch-envelope publication, repeat A0R8.4 through A0R8.7 after any
   source change.
9. On six-axis `APPROVE`, publish the tooling registry/final envelope,
   prelaunch ledger, and sole literal Attempt-8 launch envelope.
10. Require its exact eleven subject keys:
    `document_final`, `tooling_final`, `prelaunch_evidence`, `prior_attempt`,
    `build_result`, `qualification_inputs`, `powershell`, `failure_handoff`,
    `failed_candidate_tree`, `static_topology`, and `publication_policy`.
11. Require no matching validator/formal-wrapper/production-runner/Orca/
    fixed-probe process or recursive `__pycache__`, `.pyc`, or `.pyo` residue
    under A0.
12. Run the live Python `a0_tooling_approval.py generated-scope` CLI under an
    independent non-formal 180-second watchdog. Use the launch-bound bundled
    Python with `-B`, `PYTHONDONTWRITEBYTECODE=1`, the bound tooling working
    directory, and `PATH` limited to the bound PowerShell and Git directories.
    Recursively kill and wait for the process tree on timeout. Do not invoke the
    production runner or formal wrapper; prove claim/candidate absence before
    and after.
13. On both success and failure, require the same complete matching-process set
    to remain empty and no recursive Python cache residue under A0.
14. If the watchdog fails or times out, forbid formal invocation, preserve all
    Attempt-8 bytes, consume Attempt 8, and require separately reviewed
    Attempt 9. Never repair source or replace the launch envelope in place.

Verification: full approval reports Attempt 8, one authorized candidate, absent
claim, unchanged subjects, cross-gate reviewer independence, and formal
execution authorized; the live validator completes below 180 seconds without
creating a claim. A failed watchdog terminates Attempt 8 before formal work.

## A0R8.9: execute one atomic campaign

1. Rehash every launch-bound subject, the failed tree, and parent children
   immediately before invocation. Require the canonical Attempt-8 claim,
   candidate, create-once transcript
   `C:\a22or12-driver\attempt8-campaign-formal-run-v1.txt`, and repository-root
   `a0-mock-tree-<candidate-id>.json`/`.tsv` runner temporaries to be absent.
   Also require no matching validator/formal-wrapper/production-runner/Orca/
   fixed-probe process or recursive Python cache residue under A0.
2. Invoke the Attempt-8 formal wrapper exactly once.
3. The runner validates the literal launch envelope and atomically creates the
   canonical Attempt-8 claim before candidate creation.
4. Execute all frozen roles from ordinal 1 through 71 with the bound absolute
   PowerShell and existing closed child environments.
5. Never resume, retry, overwrite, select an old run, or invoke the wrapper a
   second time.
6. Retain the sole transcript and all evidence whether success or failure.

Verification: success reports 71 passed processes, 69 CLI, two direct, paired
equality, no retry/selection, no residual process, and no root/temp residue.
Failure stops Attempt 8 and requires Attempt 9.

## A0R8.10: stage, review, and publish

1. Collect only a successful Attempt-8 candidate.
2. Validate semantic coverage and workspace integrity.
3. Allocate a create-once Attempt-8 publication iteration.
4. Build corpus files twice in memory, require identical bytes, and publish
   only to the stage.
5. Assemble and validate the staged manifest.
6. Send identical stage subjects to independent fixed-source and qualification
   reviewers.
7. For `REVISE`, retain the immutable stage and create the next hash-chained
   iteration from unchanged launch subjects.
8. For terminal `REJECT`, publish only the postlaunch registry and stop.
9. For dual `APPROVE`, publish the approved stage bytes, reports, registry, and
   final sidecar envelope create-once to final paths.
10. Make the stage unavailable and run deep approved verification.

Verification: final bytes exactly equal one approved stage and resolve only
relative to the explicit final root.

## A0R8.11: release gate

Only after deep approved verification:

1. update the Task 22O progress ledger with exact identities and results;
2. release the separately reviewed adapter/engine-selection package;
3. continue tracked Rust Task 22O implementation test-first;
4. run Ares KSR exact-output verification; and
5. retain all Attempt-6, Attempt-7, and Attempt-8 evidence.

## Checklist

- [ ] Attempt-7 failure and absence facts frozen without mutation.
- [ ] Both document reviewers approved identical Attempt-8 spec/plan bytes.
- [ ] Single-scan and timeout REDs failed for the expected reasons.
- [ ] Focused tests and all 122 tests passed GREEN.
- [ ] Live generated-scope completed below unchanged 180 seconds.
- [ ] Fresh build/input/closure/candidate evidence passed independent checks.
- [ ] Six-axis tooling reviewer approved exact launch-bound subjects.
- [ ] One Attempt-8 formal campaign passed 71/71.
- [ ] Two sidecar reviewers approved identical staged bytes.
- [ ] Final deep verification passed and Package A0 released.
