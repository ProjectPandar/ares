# Plan: KSR FDM Test V4 task171 Arachne transition-end branch direction

1. Add a focused failing branched-graph test proving an increasing transition skips the descending central branch.
2. Port source central-branch counting and recursive `isGoingDown` classification into the transition-end module.
3. Run all Arachne transition tests, line-count checks, formatting, and workspace Clippy.
4. Record the source-cited slice in `docs/roadmap.md`, commit, and push independently.
