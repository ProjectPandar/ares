# Task 22O Package A0 Qualification Recovery Attempt 9 Plan

## Goal

Freeze the consumed Attempt-8 candidate, repair exact evidence JSON string
preservation test-first, rebuild every launch-bound subject in fresh A22OR14
roots, execute one new 71-role qualification campaign, require the strict
collector to pass, and publish only independently approved sidecar bytes.

## Non-negotiable boundaries

- Never rerun or overwrite the Attempt-8 wrapper, production runner,
  transcript, claim, candidate, review evidence, launch envelope, watchdog,
  or A22OR13 build/input evidence.
- Never mutate Attempt-8 leaf JSON to normalize timestamps.
- Never create an Attempt-8 retry, replacement candidate, stage, or sidecar.
- Do not relax `a0_run_validation.py` standalone/embedded equality.
- Do not change fixed Orca behavior, the 71-role order, or expected KSR records.
- The production repair is centralized string-preserving JSON parsing only.
- Before document approval, only create-once document-review iteration
  artifacts are authorized.
- Keep all edited source/test files ASCII and below 400 physical lines.
- Preserve exactly 69 source roles, 28 test paths, 122 discovered test IDs, and
  26 required test IDs.
- A formal or collector Attempt-9 failure is terminal and requires Attempt 10.
- Any sidecar `REVISE` or `REJECT` is terminal: retain the stage/reports,
  consume Attempt 9, forbid further Attempt-9 publication, and require a
  separately reviewed Attempt 10.
- Preserve the governing Task 22O spec SHA-256
  `78c44972e284eb615bf96228cbc5d0fe3a5c731a853c3b1cf518f92219b95674`
  and plan SHA-256
  `94c361d0d4c89eb5019f07f3a3e4101b8d89857d02c06629e3c794920f645e80`.
- After Package A0, keep Options 3MF-derived, forbid fixture/reference
  constants and legacy fallbacks, remove touched obsolete pinning tests, use
  real Rust/test modules below 400 LOC without source-splitting macros, and use
  the same final six-axis reviewer through every repair loop.

## A0R9.1: freeze Attempt-8 failure

1. Rehash the formal transcript, watchdog transcript, claim, launch approval,
   build result, qualification inputs, candidate `STARTED.json`, candidate
   result, 71 role specifications, and 71 run roots.
2. Verify 71 passed processes, 69 CLI roles, two direct roles, no retry, no
   selected run, and no `FAILED.json`.
3. Recompute the first strict collector rejection at ordinal `00001`,
   `env-off`, without writing candidate or publication bytes.
4. Verify all 69 CLI leaves differ only at the exact timestamp representation
   and both direct roles correctly omit datadir evidence.
5. Verify parent logical retained-attempt order is exactly
   `["8a5aab7461b877b1","11d07a6fd071c211"]`, while physical membership is the
   equal lexically sorted set
   `["11d07a6fd071c211","8a5aab7461b877b1"]`.
6. Verify no matching formal runner, validator, Orca, or fixed-probe process
   and no recursive Python cache residue remains.

Verification: two fresh-process read-only audits produce identical identities
and facts with no filesystem mutation.

## A0R9.2: approve the document frame

1. Freeze revision-1 specification and plan identities.
2. Allocate `attempt-9/document-reviews/iteration-0001`.
3. Send identical bytes to distinct governance and technical reviewers.
4. Persist both reports and a canonical review envelope create-once.
5. If either returns `REVISE`, repair only listed document findings and allocate
   the next consecutive create-once iteration.
6. Publish a hash-chained registry and final document envelope only after both
   reviewers return `APPROVE` on the same latest bytes.

Verification: every reviewed five-file iteration is immutable and chained;
reviewer identities differ; terminal reports bind identical document hashes;
and `implementation_authorized=true`.

## A0R9.3: publish the prior-attempt handoff

1. Copy the terminal Attempt-8 runner bytes create-once to
   `attempt-9/prior-attempt/attempt8-runner.ps1`.
2. Uniquely select and bind the terminal Attempt-8 source-ledger row for
   `tooling/run_fixed_qualification.ps1`.
3. Capture an empty matching-process/cache-residue set immediately before
   handoff publication.
4. Create `attempt-9/prior-attempt-v1.json` binding every subject and fact in
   specification section "Attempt-9 failure handoff".
5. Rehash all bound subjects and recompute the 69 CLI representation drifts in
   two fresh processes.
6. Reject any changed root/path pair, candidate byte, parent child set, or
   historical identity.

Verification: both handoff subjects are create-once, the live Attempt-8
evidence is unchanged, and no process, claim, candidate, or stage is created.

## A0R9.4: write the authentic RED

1. Extend the existing successful run-one lifecycle test; do not create a new
   test method.
2. Set a real datadir fixture file's last-write time to an exact second.
3. Run the real PowerShell leaf path and parse its standalone datadir and
   `result.json` evidence.
4. Assert exact before, after, and diff object equality.
5. Assert embedded before/after `earliest_last_write_utc` text retains
   `.0000000Z`.
6. Retain the focused failing transcript before production repair.
7. Record the exact pre-repair source identity and failing assertion for later
   tooling-review binding.

Verification: the one existing test ID fails specifically because default
`ConvertFrom-Json` removes the seven fractional zero digits.

## A0R9.5: implement the minimal producer repair

1. In the centralized evidence JSON reader, use
   `ConvertFrom-Json -DateKind String`.
2. Do not edit the collector, validator, hash-tree producer, timestamp
   formatter, expected fixture values, or result schema.
3. Turn the focused RED GREEN through the same real PowerShell leaf path.
4. Verify a fresh small candidate passes strict public collection.
5. Retain focused GREEN output and exact source diff.
6. Record the repaired source identity and passing assertion in the same
   provenance subject.

Verification: standalone and embedded before/after/diff objects are exactly
equal, including `.0000000Z`.

## A0R9.6: adapt current-attempt tooling

1. Change only current-attempt Python/PowerShell domains, paths, kinds,
   numbers, runner errors, claim paths, launch paths, and publication paths to
   Attempt 9.
2. Preserve every historical Attempt-7 and Attempt-8 literal and identity.
3. Validate the canonical Attempt-8 handoff and exact two-child failed history.
4. Keep launch subjects at exactly eleven, with `prior_attempt` binding the
   complete no-follow Attempt-8 candidate snapshot and nested historical
   handoff.
5. Update fixtures and static topology to 69 source paths, 28 test paths, 122
   test IDs, 26 required tests, 135 A0 paths, 31 legacy frozen prerequisites,
   and 20 exact repository documents.
6. Regenerate the Attempt-9 publication policy from final source semantics.
7. Implement exact candidate-parent state transitions in Python and
   `run_fixed_qualification.ps1`: prelaunch logical order
   `["8a5aab7461b877b1","11d07a6fd071c211"]`; postclaim logical order appends
   exactly `<attempt9_candidate_id>`; physical membership always equals the
   sorted set of the active binding.
8. Extend existing tests to reject logical-order drift, missing/extra physical
   children, aliasing, and a candidate collision with either retained ID.
9. Derive the current document iteration count from the Attempt-9 registry.

Verification: focused adaptation tests pass; no stale current-attempt Attempt-8
domain/path/kind remains; historical A8 validation still uses A8 literals.

## A0R9.7: run complete tooling verification

1. Run all 122 unit tests with bundled Python, `-B`, and bytecode disabled.
2. Run Python AST, PowerShell AST, ASCII, LOC, role-count, test-count,
   required-ID, forbidden-macro, topology, policy, and transcript checks.
3. Require every source/test file to have `0 < LOC < 400`.
4. Rehash both retained failed candidates before and after.
5. Revalidate prior-attempt handoff, parent children, and no process/cache
   residue.

Verification: one complete transcript reports 122/122 and all static markers
after every Attempt-9 adaptation.

## A0R9.8: prepare fresh A22OR14 build and inputs

1. Allocate new pairwise tree-disjoint source, build, install, and evidence
   roots that do not reuse A22OR8-A22OR13 or Attempt-6 roots.
2. Recreate the fixed-source derivative from commit/tree and patch identities.
3. Configure, build, install, and run the fixed CTest RED/GREEN grammar.
4. Regenerate all four adjacent-DLL runtime closures and probe identity.
5. Emit canonical build-result v2 and deterministic qualification-inputs v2
   using the final Attempt-9 candidate domain.
6. Require Python and PowerShell candidate IDs to agree.
7. Verify the candidate is absent and the parent contains exactly the two
   retained failed-history children under both the logical retained-attempt
   order and equal sorted physical-set contract.

Verification: production build-result/input validation passes in two fresh
processes; roots are literal, canonical, non-aliased, and disjoint.

## A0R9.9: independent build/input review

1. Send fixed-source, patch, configure/build/install, CTest, runtime-closure,
   child-environment, build-result, qualification-input, and candidate-ID
   evidence to an independent read-only reviewer.
2. Require explicit checks for provenance, closure completeness, fresh-root
   disjointness, candidate agreement, and absence.
3. If `REVISE`, fix only listed issues, allocate entirely new roots after any
   build/input-affecting source change, and re-review.

Verification: reviewer returns `APPROVE` with an empty fix list over exact
frozen identities.

## A0R9.10: tooling review and launch approval

1. Freeze static topology, publication policy, final source ledger, exact
   focused RED and GREEN outputs, RED/GREEN provenance, complete test
   transcript, prior-attempt handoff, and approved fresh build/input evidence.
2. Allocate a create-once Attempt-9 tooling-review iteration.
3. Send exact subjects to an independent six-axis reviewer covering
   requirements completeness, logical correctness, boundary cases, code
   quality, test coverage, and actual execution results.
4. Require this reviewer to differ from both document reviewers.
5. For `REVISE`, retain the iteration, repair only findings, and repeat focused
   tests, complete verification, fresh build/input preparation if invalidated,
   and review in a new iteration.
6. On `APPROVE`, publish the tooling registry/final envelope, prelaunch ledger,
   and sole Attempt-9 launch envelope create-once.
7. Require the exact eleven launch subject keys specified by the specification.
8. Verify candidate/claim/transcript absence and empty process/cache state.
9. Require the exact prelaunch logical order
   `["8a5aab7461b877b1","11d07a6fd071c211"]` and its equal sorted physical
   membership in both Python and PowerShell validation.

Verification: full approval reports Attempt 9, one authorized candidate,
unchanged launch subjects, cross-gate reviewer independence, and formal
execution authorized.

## A0R9.11: watchdog and one atomic campaign

1. Run the live Python `generated-scope` CLI under the independent non-formal
   180-second watchdog without invoking the production runner or creating a
   claim/candidate.
2. Require the watchdog to recursively terminate and wait on timeout and prove
   process/cache/claim/candidate absence before and after.
3. Rehash every launch subject and both retained failed candidates immediately
   before formal invocation.
4. Require the Attempt-9 claim, candidate, formal transcript, and runner
   temporaries to be absent.
5. Invoke the Attempt-9 formal wrapper exactly once.
6. Execute ordinals 1-71 with no resume, retry, overwrite, selection, or old
   leaf reuse.
7. Retain the transcript and all evidence whether success or failure.
8. After atomic claim/candidate creation, require logical order
   `["8a5aab7461b877b1","11d07a6fd071c211","<attempt9_candidate_id>"]` and
   physical membership equal to its sorted set.

Verification: success reports 71 passed processes, 69 CLI, two direct, paired
equality, no retry/selection, and no residue. Failure requires Attempt 10.

## A0R9.12: strict postcampaign collection

1. Invoke the existing collector read-only over only the fresh Attempt-9
   candidate.
2. Validate every leaf and all closed evidence before creating a stage.
3. Require standalone and embedded datadir before/after/diff equality in all 69
   CLI roles.
4. Require the two direct roles to omit datadir evidence.
5. Require the complete ordered corpus and unchanged candidate tree.

Verification: collector returns the full corpus without
`datadir JSON file/result drift` or any other evidence error. Rejection consumes
Attempt 9 and forbids in-place repair.

## A0R9.13: stage, review, and publish

1. Validate semantic coverage and workspace integrity.
2. Allocate a create-once Attempt-9 publication iteration.
3. Build corpus files twice in memory, require identical bytes, and publish
   only to the stage.
4. Assemble and validate the staged manifest.
5. Send identical stage subjects to independent fixed-source and qualification
   reviewers distinct from the tooling reviewer.
6. For any `REVISE`, `REJECT`, reviewer disagreement, or subject drift, retain
   the stage/reports immutably, consume Attempt 9, forbid additional Attempt-9
   stage/publication/final-sidecar creation, and require separately reviewed
   Attempt 10.
7. Only for dual `APPROVE`, publish approved stage bytes, reports, registry, and
   final sidecar create-once.
8. Make the stage unavailable and run deep approved verification.

Verification: final bytes exactly equal one approved stage and resolve only
relative to the explicit final root.

## A0R9.14: release and resume Task 22O

Only after deep approved verification:

1. update the Task 22O progress ledger with exact identities and results;
2. release the separately reviewed adapter/engine-selection package;
3. continue tracked Rust Task 22O implementation one Option slice at a time;
4. derive every Option solely from the supplied 3MF;
5. run Ares KSR exact-output verification; and
6. send the exact tracked diff and fresh execution evidence to one reviewer
   independent of the implementing main thread for six axes: requirement
   completeness, logical correctness, boundary cases, code quality, test
   coverage, and actual execution results;
7. receive an ordered fix list in the main thread, repair it, and send the new
   exact diff/evidence to the same reviewer identity repeatedly until all axes
   return `APPROVE` or a precise blocker is recorded; and
8. retain all Attempt-6 through Attempt-9 evidence.

## Checklist

- [ ] Attempt-8 candidate and collector failure frozen without mutation.
- [ ] Two document reviewers approved identical Attempt-9 spec/plan bytes.
- [ ] Exact timestamp-preservation RED failed for the expected reason.
- [ ] Minimal producer fix turned RED GREEN without validator changes.
- [ ] All 122 tests and static checks passed.
- [ ] Fresh A22OR14 build/input evidence passed independent review.
- [ ] Six-axis tooling reviewer approved exact launch-bound subjects.
- [ ] Watchdog passed and one Attempt-9 campaign passed 71/71.
- [ ] Strict collector passed all 69 CLI and two direct leaves.
- [ ] Two sidecar reviewers approved identical staged bytes.
- [ ] No sidecar rejection/revision was continued inside Attempt 9.
- [ ] Deep verification passed and Package A0 released.
- [ ] Independent final Rust six-axis review and same-reviewer repair loop
      reached APPROVE, or a precise blocker was recorded.
