# Task 22O.10 implementation plan

1. Fix the source boundary at `PerimeterGenerator.cpp:1451-1569` and record O1's transactional proof that all reorientation/reordering branches are inactive.
2. Add typed inactive provenance for overhang reorientation, accepted `InnerOuter`, and each false layer-zero outer-brim predicate reason.
3. Move each nonempty O9 flat collection into one nested perimeter collection; omit empty collections and preserve the boxed O5 predecessor and allocations.
4. Wire O10 into the internal preparation and public incomplete lifecycle with an iterative terminal sink.
5. Add direct empty/nonempty, nesting, allocation, and provenance tests plus exact in-memory KSR structure/checksum and lifecycle anchors.
6. Update architecture and roadmap, then run focused O5-O10, workspace Nextest, strict Clippy/check, WASM, rustfmt, diff, LOC, and forbidden-pattern gates.

Active overhang reorientation, outer-first/brim reversal, sandwich ordering, gap fill at line 1573+, seams, infill, motion, and G-code remain deferred.
