# M93: PrintConfig machine speed and acceleration limit option registry

## Goal
Port the adjacent machine XYZE maximum speed and maximum acceleration option-definition slice generated by `libslic3r::PrintConfigDef::init_fff_params` in `OrcaSlicer/src/libslic3r/PrintConfig.cpp:4377-4428` into `ares-core` registry metadata.

## Rewrite gate
This milestone is governed by `docs/architecture/ard-0020-future-milestone-rewrite-gate.md`. Its source boundary is `PrintConfig.hpp:1249-1257`, `PrintConfig.cpp:4377-4428`, and the current option registry metadata boundary; no new Ares pipeline, crate, dependency, machine-limit emission behavior, firmware G-code behavior, UI behavior, slicing behavior, extrusion behavior, or G-code behavior is added.

## Exit checklist
- `OPTION_DEFINITIONS` includes `machine_max_acceleration_e`, `machine_max_acceleration_x`, `machine_max_acceleration_y`, `machine_max_acceleration_z`, `machine_max_speed_e`, `machine_max_speed_x`, `machine_max_speed_y`, and `machine_max_speed_z` with exact kinds, defaults, and source line ranges.
- `OPTION_DEFINITIONS` remains a single sorted slice for `option_definition()` binary-search lookup.
- `known_definition_count()` counts newly registered keys while preserving unknown options.
- Public lookup coverage exists without growing any modified Rust file beyond 400 LOC.
- `late` remains below the 400 LOC threshold by splitting later sorted registry definitions into a focused `late_tail` shard without changing registry behavior.
- Upstream UI full-label/tooltip/category/sidetext/min/mode/readonly metadata remains deferred beyond the current metadata boundary.
- Runtime behavior for machine-limit emission, M201/M203 G-code emission, firmware profile behavior, slicing, extrusion, and downstream G-code behavior remains deferred.
- Machine jerk, junction-deviation, min-rate, acceleration P/R/T, resonance, input-shaping, and following options from `PrintConfig.cpp:4429+` remain unchanged/deferred.
- Modified Rust files remain under 400 LOC.
- `cargo fmt --check`, `cargo test`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo check -p ares-core --target wasm32-unknown-unknown`, and `git diff --check` pass.
