# M151: PrintConfig filament extruder override registry

## Goal
Port the generated filament extruder override option-definition slice from `libslic3r::PrintConfigDef::init_fff_params` in `OrcaSlicer/src/libslic3r/PrintConfig.cpp:7121-7156` into `ares-core` registry metadata.

## Rewrite gate
This milestone is governed by `docs/architecture/ard-0020-future-milestone-rewrite-gate.md`. Its source boundary is `PrintConfig.hpp:512`, `PrintConfig.cpp:63-83`, `PrintConfig.cpp:7121-7156`, and the raw extruder option definitions copied by that loop. No new Ares pipeline, crate, dependency, filament override resolution behavior, nullable value merging behavior, UI behavior, slicing behavior, geometry behavior, extrusion behavior, or G-code writer behavior is added.

## Exit checklist
- `OPTION_DEFINITIONS` includes all 16 `filament_extruder_override_keys` generated by the upstream loop with nullable kinds, inherited defaults, and source line ranges.
- `OPTION_DEFINITIONS` remains a single sorted slice for `option_definition()` binary-search lookup.
- `known_definition_count()` counts the covered keys while preserving unknown options.
- Public lookup coverage exists without growing any modified Rust file to 400 LOC or above.
- Runtime behavior for filament override resolution, nullable override following, retraction planning, slicing, geometry, extrusion, and downstream G-code remains unchanged/deferred.
- `detect_narrow_internal_solid_infill` from `PrintConfig.cpp:7158-7165` remains unchanged/deferred.
- `cargo fmt --check`, `cargo test`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo check -p ares-core --target wasm32-unknown-unknown`, and `git diff --check` pass.
